# Task Completion Checklist

Before considering any change done, run both (order doesn't matter, but build catches type errors lint won't):
1. `npm run build` — must succeed (tsc -b + vite build)
2. `npm run lint` — must be clean

There are no unit/integration tests in this repo.
